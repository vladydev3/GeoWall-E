
f(u)=
let
    circle b;
    line c;
    point t;
    segment e;
    point g;
    line yg;
    line yj;

    draw b;
    draw c;
    draw t;
    draw e;
    draw g;
    draw yg;
    draw yj;
in if u>0 then f(u-1)else 0;

trtrt= f(3);